<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:extended="clr-namespace:UraniumUI.Material.Controls;assembly=YOUR_ASSEMBLY">

    <VerticalStackLayout
        Padding="24"
        Spacing="20">

        <extended:AlignedEditorField
            Title="Aligned editor"
            Text="Centered text"
            HeightRequest="90"
            HorizontalTextAlignment="Center"
            VerticalTextAlignment="Center" />

        <!-- Recommended MissionPlanner parameter usage:
             Bind Text when the current ViewModel value is a string. -->
        <extended:NumericUpDownField
            Title="Number"
            Text="{Binding Value, Mode=TwoWay}"
            Min="{Binding Minimum}"
            Max="{Binding Maximum}"
            StepSize="{Binding StepSize}"
            WidthRequest="275"
            HorizontalTextAlignment="Center"
            VerticalTextAlignment="Center"
            ButtonOrientation="Horizontal"
            BorderColor="Aqua" />

        <!-- Bind Value instead when the ViewModel exposes a double. -->
        <extended:NumericUpDownField
            Title="Altitude"
            Value="{Binding Altitude, Mode=TwoWay}"
            Min="0"
            Max="10000"
            StepSize="0.5"
            DecimalPlaces="1"
            CultureName="da-DK"
            ButtonOrientation="Vertical"
            HorizontalTextAlignment="End"
            VerticalTextAlignment="Center"
            WidthRequest="220" />

    </VerticalStackLayout>
</ContentPage>
