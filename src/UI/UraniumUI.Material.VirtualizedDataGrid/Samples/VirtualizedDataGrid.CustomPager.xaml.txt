<!-- Optional custom pager. The PagerTemplate binding context is the VirtualizedDataGrid. -->
<virtualized:VirtualizedDataGrid
    ItemsSource="{Binding Parameters}"
    EnablePaging="True"
    ShowPager="True"
    PageSize="100">

    <virtualized:VirtualizedDataGrid.PagerTemplate>
        <DataTemplate>
            <Grid ColumnDefinitions="*,Auto" Padding="8,6" ColumnSpacing="24">
                <HorizontalStackLayout Grid.Column="0" Spacing="8">
                    <Button Text="First" Command="{Binding FirstPageCommand}" />
                    <Button Text="Previous" Command="{Binding PreviousPageCommand}" />
                    <Label Text="{Binding CurrentPage, StringFormat='Page {0}'}"
                           VerticalOptions="Center" />
                    <Label Text="{Binding TotalPageCount, StringFormat='of {0}'}"
                           VerticalOptions="Center" />
                    <Button Text="Next" Command="{Binding NextPageCommand}" />
                    <Button Text="Last" Command="{Binding LastPageCommand}" />
                </HorizontalStackLayout>

                <HorizontalStackLayout Grid.Column="1" Spacing="8">
                    <Label Text="Rows per page:" VerticalOptions="Center" />
                    <Picker ItemsSource="{Binding PageSizeOptions}"
                            SelectedItem="{Binding PageSize, Mode=TwoWay}"
                            WidthRequest="92" />
                    <Label Text="{Binding FilteredItemCount, StringFormat='Matching: {0}'}"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Grid>
        </DataTemplate>
    </virtualized:VirtualizedDataGrid.PagerTemplate>

    <virtualized:VirtualizedDataGrid.Columns>
        <!-- columns -->
    </virtualized:VirtualizedDataGrid.Columns>
</virtualized:VirtualizedDataGrid>
