<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:navigation="clr-namespace:MissionPlanner.App.Navigation"
    x:ClassModifier="internal">

    <!--
        Application overrides are merged after UraniumUI's Material StyleResource.
        Keep the override narrow: use the shared semantic palette rather than
        introducing control-specific hard-coded colors.
    -->

    <Style TargetType="Button" BaseResourceKey="UraniumUI.Styles.Button" CanCascade="True">

        <Setter Property="Shadow"
                Value="{StaticResource UraniumUI.Styles.Resources.ShadowElevation1}" />
        <Setter Property="TextColor"
                Value="{DynamicResource OnPrimary}" />
        <Setter Property="BackgroundColor"
                Value="{DynamicResource Primary}" />

        <Setter Property="FontFamily" Value="OpenSansRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="14,10" />

        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />

                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Shadow"
                                    Value="{StaticResource UraniumUI.Styles.Resources.ShadowElevation0}" />
                            <Setter Property="TextColor"
                                    Value="{DynamicResource DisabledText}" />
                            <Setter Property="BackgroundColor"
                                    Value="{DynamicResource DisabledBackground}" />
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="Hover">
                        <VisualState.Setters>
                            <Setter Property="Shadow"
                                    Value="{StaticResource UraniumUI.Styles.Resources.ShadowElevation2}" />
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Shadow"
                                    Value="{StaticResource UraniumUI.Styles.Resources.ShadowElevation0}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="ColumnHeaderIconStyle" TargetType="Image">
        <Setter Property="WidthRequest" Value="28" />
        <Setter Property="HeightRequest" Value="28" />
        <Setter Property="Aspect" Value="AspectFit" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style TargetType="uranium:Select">
        <Setter Property="DropDownBackgroundColor"
                Value="{DynamicResource Surface}" />
        <Setter Property="DropDownBorderColor"
                Value="{DynamicResource Outline}" />
        <Setter Property="SelectedItemBackgroundColor"
                Value="{DynamicResource PrimaryContainer}" />
        <Setter Property="HoveredItemBackgroundColor"
                Value="{DynamicResource SurfaceVariant}" />
        <Setter Property="PressedItemBackgroundColor"
                Value="{DynamicResource PrimaryContainer}" />
    </Style>

</ResourceDictionary>
