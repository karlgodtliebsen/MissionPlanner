<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    x:Class="UraniumUI.Material.VirtualizedDataGrid.Resources.StyleResource"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:UraniumUI.Material.VirtualizedDataGrid.Controls">

    <ResourceDictionary.MergedDictionaries>
        <!--
            Keep styles in a replaceable child dictionary, matching UraniumUI's
            own StyleResource. When ColorsOverride changes, the code-behind can
            remove this dictionary and initialize it again without adding a
            second implicit type-keyed style to the root dictionary.
        -->
        <ResourceDictionary>
            <Style
                x:Key="UraniumUI.Styles.VirtualizedDataGrid"
                TargetType="controls:VirtualizedDataGrid"
                ApplyToDerivedTypes="True">
                <Setter
                    Property="BackgroundColor"
                    Value="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}" />
                <Setter
                    Property="LineSeparatorColor"
                    Value="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}" />
                <Setter
                    Property="Stroke"
                    Value="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}" />
                <Setter Property="StrokeShape" Value="RoundRectangle 8" />
                <Setter Property="StrokeThickness" Value="0.5" />
                <Setter
                    Property="SelectionColor"
                    Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
            </Style>

            <!-- Implicitly applies the Material appearance to every virtualized grid. -->
            <Style
                TargetType="controls:VirtualizedDataGrid"
                ApplyToDerivedTypes="True"
                BasedOn="{StaticResource UraniumUI.Styles.VirtualizedDataGrid}" />
        </ResourceDictionary>
    </ResourceDictionary.MergedDictionaries>

</ResourceDictionary>
